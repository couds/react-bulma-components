import { Meta, ArgsTable } from '@storybook/addon-docs/blocks';
import { Block, Card, Columns, Container, Message, Notification, Table, Element } from '../../..';

<Meta title="Core Component/PropTypes" />

# The Element component
### Common Props

All (Or almost all) helpers and modifiers of bulma are implemented as a prop inside the
Element component (and on every component on the react-bulma-component library), here you
can find a list of all common props between all components


<ArgsTable of={Element} />
