import { ArgsTable, Canvas, Meta, Story } from '@storybook/addon-docs/blocks';
import { Form } from '../..';
import * as stories from './checkbox.story';

<Meta title="Form/Checkbox" />

# Checkbox

<Canvas>
  <Story story={stories.BasicExample} />
</Canvas>

A simple un-styled checkbox.

## Props

Note that props are forwarded to the checkbox input itself excluding the label.

<ArgsTable of={Form.Checkbox} />

## Label

You can add put any component as the label of the checkbox.

<Canvas>
  <Story story={stories.CustomLabelComponent} />
</Canvas>
